import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  footer: {
    height: 130,
    padding: 40,
    display: 'flex',
    flexDirection: 'row',
    alignItems: 'flex-end'
  },
  footerText: {
    color: 'white',
  },
});

export default styles;
