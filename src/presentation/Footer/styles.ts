import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  footer: {
    height: 100,
    padding: 40,
    backgroundColor: '#bbb',
    display: 'flex',
    flexDirection: 'row',
  },
});

export default styles;
